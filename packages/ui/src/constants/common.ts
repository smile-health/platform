export enum STATUS {
  ACTIVE = 1,
  INACTIVE = 0,
}
export const PAGE_SIZE = [10, 25, 50, 100]

export enum BOOLEAN {
  TRUE = 1,
  FALSE = 0,
}

export const MAX_LIMIT_DIGIT_NUMBER = 12

export const PHONE_REGEX = /^(?:\+\d{7,14}|\d{8,15})$/
